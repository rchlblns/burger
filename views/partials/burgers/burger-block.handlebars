<div class="input-group">
    <li class="form-control text-center">
        {{burger_name}}
        {{#unless devoured}}
            <button class="devour-burger btn-success" data-id="{{id}}" data-newdevour="{{this.devoured}}">Eat it!</button>
        {{/unless}}
    </li>
</div>